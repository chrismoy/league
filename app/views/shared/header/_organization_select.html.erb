<%= select_tag "park-select",
    options_from_collection_for_select( @organizations ,
    "id",
    "name",
    logged_in? ? current_user.organization_id : Organization.first.id ),
    class: "organization-select"
%>
