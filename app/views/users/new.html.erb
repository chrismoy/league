<% provide(:title, "Sign Up") %>
<h1>Sign Up</h1>

<div class="row">
  <div class="small-12 columns">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create my account", class: "button expand" %>
    <% end %>
  </div>
</div>